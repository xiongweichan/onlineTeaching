<UserControl x:Class="NBSchool.WPFClient.Views.LiveCenterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NBSchool.WPFClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <RadioButton Content="我的直播" IsChecked="{Binding ShowMyLive}" />
            <RadioButton Content="申请直播" IsChecked="{Binding ShowRequestLive}" />
        </StackPanel>
        <ContentControl  x:Name="ActiveItem" Grid.Row="1"/>
    </Grid>
</UserControl>
