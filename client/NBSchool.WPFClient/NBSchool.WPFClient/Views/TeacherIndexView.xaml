<UserControl x:Class="NBSchool.WPFClient.Views.TeacherIndexView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NBSchool.WPFClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <RadioButton Content="信息聚合" IsChecked="{Binding ShowSummaryInfo}" GroupName="teacherindex"/>
            <RadioButton Content="讲师信息" IsChecked="{Binding ShowTeacherInfo}" GroupName="teacherindex"/>
            <RadioButton Content="身份认证" IsChecked="{Binding ShowCertification}" GroupName="teacherindex"/>
        </StackPanel>
        <ContentControl  x:Name="ActiveItem" Grid.Row="1"/>
    </Grid>
</UserControl>
