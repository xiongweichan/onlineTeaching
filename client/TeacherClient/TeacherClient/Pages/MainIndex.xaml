<UserControl x:Class="TeacherClient.Pages.MainIndex"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TeacherClient"
             mc:Ignorable="d" Loaded="UserControl_Loaded"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Height="38" Background="{StaticResource Background11}">
            <StackPanel Orientation="Horizontal" RadioButton.Checked="StackPanel_Checked">
                <RadioButton x:Name="rb_info" Tag="1" Content="信息聚合" Style="{StaticResource RadioButtonStyle3}" Margin="10,0,0,0"/>
                <RadioButton Tag="2" Content="讲师信息" Style="{StaticResource RadioButtonStyle3}" Margin="40,0,0,0"/>
                <RadioButton Tag="3" Content="身份认证" Style="{StaticResource RadioButtonStyle3}" Margin="40,0,0,0"/>
            </StackPanel>
        </Grid>
        <Frame Grid.Row="1" x:Name="frame"/>
    </Grid>
</UserControl>
